export enum Gender {
    Female = 0,
    Male = 1,
    Other = 2
}

export enum Role {
    Administrator = 0,
    Manager = 1,
    Client = 2
}

export enum AddressOwner {
    Seller = 0,
    Buyer = 1
}

export enum FavoriteType {
    Like = 0,
    Cart = 1
}
